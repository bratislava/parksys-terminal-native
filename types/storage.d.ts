export enum SecureStorageTypes {
  azureAccessToken = 'azureAccessToken',
  azureRefreshToken = 'azureRefreshToken',
  selectedUdr = 'selectedUdr',
  udrs = 'udrs',
  sessionId = 'sessionId',
}
export enum AsyncStorageTypes {
  udrs = 'udrs',
}
